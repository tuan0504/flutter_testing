name: core
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  cached_network_image: ^3.3.0
  dartz: ^0.10.1
  envied: ^0.5.4+1
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  http: ^1.1.0
  movies:
    path: ../movies
  sqflite: ^2.3.0
  
dev_dependencies:
  build_runner: ^2.4.6
  envied_generator: ^0.5.4+1
  flutter_lints: ^3.0.1
  flutter_test:
    sdk: flutter
  mockito: ^5.4.3

flutter: null